export enum endpoints {
  HOST = "http://127.0.0.1:8000/",
  POST_MAKE_PREDICTION = "predictions/",
  GET_MAKE_PREDICTION = "predictions/",
}

export enum routes {
  PREDICTOR = "/",
  PREDICTIONS = "/predictions",
}
